first_number = int(input("Enter the first integer (numerator): "))
second_number = int(input("Enter the second integer (denominator): "))
if second_number != 0:
    print("can divide by zero")
else:
    print("Cannot divide by zero")
